# actionscore/actions pipeline

trigger:
- master

jobs:
- template: 'build-binary-template.yml'
  parameters:
    pool: macOS
    binaryName: action
- template: 'build-binary-template.yml'
  parameters:
    pool: macOS
    binaryName: assigner
- template: 'build-binary-template.yml'
  parameters:
    pool: macOS
    binaryName: controller