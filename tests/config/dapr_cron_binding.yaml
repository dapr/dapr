apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: schedule
spec:
  type: bindings.cron
  version: v1
  metadata:
  - name: schedule
    envRef: CRON_SCHEDULE
  - name: direction
    value: input
