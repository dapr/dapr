apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: omithealthchecksconfig
spec:
  features:
  - name: SchedulerReminders
    enabled: false
  logging:
    apiLogging:
      omitHealthChecks: true
