apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: c-topic-subscription
spec:
  pubsubname: messagebus
  topic: pubsub-c-topic-http
  routes:
    default: /pubsub-c-topic-http
scopes:
  - pubsub-subscriber