apiVersion: v1
kind: Secret
metadata:
  name: redissecret
type: Opaque
stringData:
  # Redis is installed in the test environment namespace; use FQDN so test apps in other namespaces can connect.
  host: "dapr-redis-master.${DAPR_TEST_ENV_NAMESPACE}.svc.cluster.local:6379"
