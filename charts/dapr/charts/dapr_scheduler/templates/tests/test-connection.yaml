apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dapr_scheduler.fullname" . }}-test-connection"
  labels:
    {{- include "dapr_scheduler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dapr_scheduler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
