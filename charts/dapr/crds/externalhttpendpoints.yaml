
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.5.0
  creationTimestamp: null
  name: externalhttpendpoints.dapr.io
spec:
  group: dapr.io
  names:
    kind: ExternalHTTPEndpoint
    listKind: ExternalHTTPEndpointList
    plural: externalhttpendpoints
    singular: externalhttpendpoint
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ExternalHTTPEndpoint describes an Dapr ExternalHTTPEndpoint type to allow for external service invocation.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          auth:
            description: Auth represents authentication details for the component.
            properties:
              secretStore:
                type: string
            required:
            - secretStore
            type: object
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          scopes:
            items:
              type: string
            type: array
          spec:
            description: ExternalHTTPEndpointSpec describes an access specification for allowing external service invocations.
            properties:
              allowed:
                items:
                  description: ExternalAPISpec describes the configuration for Dapr API communication with external services.
                  properties:
                    baseUrl:
                      type: string
                    headers:
                      additionalProperties:
                        type: string
                      type: object
                    name:
                      type: string
                    protocol:
                      type: string
                  required:
                  - baseUrl
                  - name
                  type: object
                type: array
              metadata:
                items:
                  description: MetadataItem is a name/value pair for a metadata.
                  properties:
                    name:
                      type: string
                    secretKeyRef:
                      description: SecretKeyRef is a reference to a secret holding the value for the metadata item. Name is the secret name, and key is the field in the secret.
                      properties:
                        key:
                          type: string
                        name:
                          type: string
                      required:
                      - key
                      - name
                      type: object
                    value:
                      description: DynamicValue is a dynamic value struct for the component.metadata pair value.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - name
                  type: object
                type: array
            required:
            - metadata
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []