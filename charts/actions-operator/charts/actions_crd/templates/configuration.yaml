apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: configurations.actions.io
  namespace: {{ .Release.Namespace }}
spec:
  group: actions.io
  version: v1alpha1
  names:
    kind: Configuration
    plural: configurations
    singular: configuration
    categories:
    - all
    - actions
  scope: Namespaced
