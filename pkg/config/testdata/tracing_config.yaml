apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: daprsystem
  namespace: default
spec:
  tracing:
    otel:
      timeout:
        duration: 5s
      headers:
        - name: header1
          secretKeyRef:
            name: test-secret
            key: header1
        - name: header2
          secretKeyRef:
            name: test-secret
            key: header2
